services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    expose:
      - "8000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      - backend
