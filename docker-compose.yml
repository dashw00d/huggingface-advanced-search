services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    expose:
      - "8000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "${PORT:-80}:80"
    depends_on:
      - backend
