# Optional: Hugging Face API token for higher rate limits / private models
HF_TOKEN=

# Optional: Additional allowed CORS origins (comma-separated)
# Only needed if accessing the API directly (not through the nginx proxy)
ALLOWED_ORIGINS=
